{% if admin.hasRoute('duplicate') %}
    <a href="{{ admin.generateObjectUrl('duplicate', object) }}" class="btn btn-sm btn-default" title="{{ 'Duplicate'|trans({}, 'LibrinfoEmailBundle') }}">
        <i class="fa fa-files-o"></i>
        {{ 'action_duplicate'|trans({}, 'LibrinfoEmailBundle') }}
    </a>
{% endif %}
