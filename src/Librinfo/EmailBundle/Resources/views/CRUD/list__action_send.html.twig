{% if admin.hasRoute('send') and not object.sent %}
    <a href="{{ admin.generateObjectUrl('send', object) }}" class="btn btn-sm btn-default" title="{{ 'Send'|trans({}, 'SonataAdminBundle') }}">
        <i class="fa fa-paper-plane"></i>
        {{ 'action_send'|trans({}, 'SonataAdminBundle') }}
    </a>
{% endif %}
