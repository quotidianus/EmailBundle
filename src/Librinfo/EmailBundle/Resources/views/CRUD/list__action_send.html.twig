{% if admin.hasRoute('send') %}
    <a href="{{ admin.generateObjectUrl('send', object) }}" class="btn btn-sm btn-default delete_link" title="{{ 'action_send'|trans({}, 'SonataAdminBundle') }}">
        <i class="fa fa-paper-plane"></i>
        {{ 'action_send'|trans({}, 'SonataAdminBundle') }}
    </a>
{% endif %}
